import HomeSegment from "@/components/HomeSegment/HomeSegment";

const Home = () => {
  return (
    <main className="bg-gradient-pastel-home h-screen w-screen text-zinc-800">
      <HomeSegment />
    </main>
  );
};

export default Home;
